{
  "name": "jpi-exporter-web",
  "version": "1.0.0",
  "description": "A tool to export JPI schedule as an Excel file.",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --public-url ./ --no-source-maps",
    "clean": "rm -rf .parcel-cache dist",
    "deploy": "npm run clean && npm run build && gh-pages -d dist"
  },
  "author": "Ihsan Topaloglu",
  "license": "UNLICENSED",
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^6.5.1",
    "@parcel/transformer-sass": "^2.10.3",
    "bulma": "^0.9.4",
    "eslint": "^8.55.0",
    "file-saver": "^2.0.5",
    "gh-pages": "^6.1.0",
    "parcel": "^2.10.3",
    "exceljs": "^4.4.0"
  }
}
